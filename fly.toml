app = "restockpulse-app"
primary_region = "lhr"

[build]

[env]
PORT = "3000"

[http_service]
internal_port = 3000
force_https = true
auto_stop_machines = false
auto_start_machines = true
min_machines_running = 1
processes = ["app"]

[[mounts]]
source = "restockpulse_data"
destination = "/data"

[[vm]]
memory = "1gb"
cpu_kind = "shared"
cpus = 1